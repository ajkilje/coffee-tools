import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.flourfeine.coffeetools',
  appName: 'Coffee Tools',
  webDir: 'dist'
};

export default config;
